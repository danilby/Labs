

#include <iostream>

int main()
{
    int i = 123;
    int iS = sizeof(i);// Размер инта в байтах

    short sh = 10;
    int shS = sizeof(sh);// Размер шорта в байтах

    long l = 12345;
    int lS = sizeof(l);// Размер лонга в байтах

    char ch = 'a';
    int chS = sizeof(ch);// Размер чара в байтах

    float f = 12.234;
    int fS = sizeof(f);// Размер флота в байтах

    double d = 1412.12;
    int dS = sizeof(d);// Размер дабла в байтах

}

